# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Virtual environments
venv/
venv313/
ENV/
env/

# Runtime data
data/.mcp_server.pid
data/.mcp_server_http.pid
data/.mcp_server_sse.pid
data/.mcp_server_sse.lock
data/.mcp_server.lock
data/.last_active_agent
data/.metadata.lock
data/locks/
data/processes/

# Local Claude/Cursor settings (never commit credentials / machine-local perms)
.claude/settings.local.json
*.log

# Session history files (archived, not tracked)
data/*history*.json
data/*history*.csv
data/history/
data/claude_*.json
data/claude_*.csv
data/composer_*.json
data/composer_*.csv
data/cursor_*.json
data/cursor_*.csv
data/hackathon_*.json
data/hackathon_*.csv
data/portal_*.json
data/portal_*.csv
data/archive/
# Agent state files (runtime data, not tracked)
data/agents/
# Knowledge layer (may contain sensitive discoveries)
data/knowledge/
# Dialectic sessions (runtime data)
data/dialectic_sessions/
data/dialectic_sessions_archive/

# Archives (backups, old data — all runtime)
data/agents_archived/
data/archives/
data/backups/
data/analysis/
data/test_files/
data/exports/
data/mcp_server_sse*.log

# IDE
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# Testing
.pytest_cache/
.coverage
htmlcov/
.tox/

# Jupyter
.ipynb_checkpoints

# Environment variables
.env
.env.local
secrets/

# Internal/local documentation (deployment-specific configs)
docs/INTERNAL_CONFIG.md
*.local.md

# macOS
.DS_Store
.AppleDouble
.LSOverride

# Temporary files
*.tmp
*.bak
*.old

# Agent metadata (contains API keys)
data/agent_metadata.json
data/agent_metadata.json.bak

# SQLite databases (runtime data)
data/*.db
data/*.db-shm
data/*.db-wal
data/*.db-journal
data/*.db-shm.__migrated__
data/*.db-wal.__migrated__

# Activity tracking (runtime data)
data/activity/

# Calibration backups
data/calibration_state.json
data/calibration_state_backup_*.json

# Knowledge layer (may contain sensitive discoveries)
data/knowledge/
data/knowledge_graph.json

# Audit logs (may contain sensitive information)
data/audit_log.jsonl
data/tool_usage.jsonl

# Test artifacts (keep test files, ignore outputs)
*.pyc
.pytest_cache/
.coverage
coverage.xml
htmlcov/
*.lock
tests/security_audit_report.json
tests/additional_security_report.json

# Prevent test script proliferation in root (tests must go in tests/)
/test_*.py
/demo_*.py
.governance_session
.mcp_session
config/generated-*.json
data/telemetry/*.jsonl
