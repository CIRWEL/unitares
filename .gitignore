# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Virtual environments
venv/
venv313/
ENV/
env/

# Runtime data (preserve agent data but exclude temp files)
data/.mcp_server.pid
data/.mcp_server_http.pid
data/.mcp_server.lock
data/locks/
data/processes/
*.log

# Session history files (archived, not tracked)
data/*history*.json
data/*history*.csv
data/history/
data/claude_*.json
data/claude_*.csv
data/composer_*.json
data/composer_*.csv
data/cursor_*.json
data/cursor_*.csv
data/hackathon_*.json
data/hackathon_*.csv
data/portal_*.json
data/portal_*.csv
data/archive/
# Agent state files (runtime data, not tracked)
data/agents/
# Knowledge layer (may contain sensitive discoveries)
data/knowledge/
# Dialectic sessions (runtime data)
data/dialectic_sessions/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# Testing
.pytest_cache/
.coverage
htmlcov/
.tox/

# Jupyter
.ipynb_checkpoints

# Environment variables
.env
.env.local
secrets/

# macOS
.DS_Store
.AppleDouble
.LSOverride

# Temporary files
*.tmp
*.bak
*.old

# Lock files (runtime)
data/.metadata.lock

# Agent metadata (contains API keys - DO NOT COMMIT!)
data/agent_metadata.json
data/agent_metadata.json.bak

# SQLite databases (runtime data with API keys - DO NOT COMMIT!)
data/*.db
data/*.db-shm
data/*.db-wal
data/*.db-journal
data/.last_active_agent
data/.mcp_server_sse.pid

# Activity tracking (runtime data)
data/activity/*.json
data/archives/*.json
data/exports/*.json

# Calibration backups
data/calibration_state.json
data/calibration_state_backup_*.json

# Knowledge layer (may contain sensitive discoveries)
data/knowledge/
data/knowledge_graph.json

# Audit logs (may contain sensitive information)
data/audit_log.jsonl
data/tool_usage.jsonl

# Test artifacts (keep test files, ignore outputs)
*.pyc
.pytest_cache/
.coverage
coverage.xml
htmlcov/
*.lock
tests/security_audit_report.json
tests/additional_security_report.json

# Prevent test script proliferation in root (tests must go in tests/)
/test_*.py
/demo_*.py
