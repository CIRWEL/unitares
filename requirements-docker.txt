# Docker dependencies for UNITARES Governance MCP
#
# requirements-full.txt minus:
# - pytest* (not needed in production)
# - sentence-transformers (saves ~1.3GB, falls back to PG FTS)
# - requests (only used by integration tests)
#
# Install:
#   pip install -r requirements-docker.txt

# Core MCP SDK
mcp>=1.26.0,<2.0.0

# Governance system core math
numpy>=1.24.0,<3.0.0

# HTTP server support (multi-client mode)
uvicorn>=0.30.0
starlette>=0.37.0
websockets>=12.0

# Operational dependencies
psutil>=5.9.0
aiofiles>=23.2.1
orjson>=3.9.0

# Metrics and monitoring
prometheus-client>=0.19.0

# PostgreSQL + AGE support
asyncpg>=0.29.0

# Redis cache support
redis>=5.0.0

# Environment variable loading
python-dotenv>=1.0.0
