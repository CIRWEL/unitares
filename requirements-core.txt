# Core (minimal) dependencies for UNITARES Governance MCP
#
# Use this for:
# - stdio MCP server (`src/mcp_server_std.py`)
# - most local development where you don't need SSE/HTTP
#
# Install:
#   pip install -r requirements-core.txt

# Core MCP SDK (1.24.0+ includes Tasks, Elicitation, Streamable HTTP)
mcp>=1.24.0

# Governance system core math
numpy>=1.24.0


