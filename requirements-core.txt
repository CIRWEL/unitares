# Core (minimal) dependencies for UNITARES Governance MCP
#
# Use this for:
# - thin stdio / proxy-oriented setups
# - environments where you are not running the full local HTTP server
#
# Install:
#   pip install -r requirements-core.txt

# Core MCP SDK
mcp>=1.26.0,<2.0.0

# Governance system core math
numpy>=1.24.0,<3.0.0

# EISV dynamics engine — public GitHub, compiled at install time via Cython.
# Pinned by tag so upstream changes don't break the server unexpectedly;
# bump the tag here when unitares-core releases a new version.
#
# Not on PyPI yet. If you want a pre-built wheel, build locally:
#   git clone https://github.com/CIRWEL/unitares-core.git
#   cd unitares-core && pip install -e .
#
# To run without unitares-core (behavioral-only mode, no ODE):
#   export UNITARES_DISABLE_ODE=1
unitares-core @ git+https://github.com/CIRWEL/unitares-core.git@v2.3.0

