# Full dependencies for UNITARES Governance MCP
#
# Use this for:
# - HTTP server (`src/mcp_server.py`)
# - multi-client deployments (Cursor + Claude Desktop sharing state)
# - tests / diagnostics scripts
#
# Install:
#   pip install -r requirements-full.txt

-r requirements-core.txt

# HTTP server support (multi-client mode)
uvicorn>=0.30.0
starlette>=0.37.0

# Operational dependencies (optional but recommended)
psutil>=5.9.0
aiofiles>=23.2.1
orjson>=3.9.0

# Metrics and monitoring
prometheus-client>=0.19.0

# PostgreSQL + AGE support (optional - for production deployment)
asyncpg>=0.29.0

# Redis cache support (optional - for distributed session cache + locking)
redis>=5.0.0

# Semantic search support (optional - for knowledge graph semantic queries)
sentence-transformers>=2.2.0

# Numerical computation (used by governance_monitor, calibration, etc.)
numpy>=1.24.0

# HTTP client (used by integration tests)
requests>=2.31.0

# Testing and coverage (optional but included for convenience)
pytest>=7.4.0
pytest-cov>=4.1.0
pytest-asyncio>=0.21.0

# Environment variable loading
python-dotenv>=1.0.0


